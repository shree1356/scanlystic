Scanlystic — dev & build commands

# Install deps (frontend)
npm install

# Run dev server (frontend)
npm run dev
# open the url printed (usually http://localhost:5173)

# Build production bundle
npm run build

# Preview production build locally
npm run preview

# If you later use the backend:
# cd ../scanlystic-backend
# npm install
# node index.js